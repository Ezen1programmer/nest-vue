// Is this needed?
// https://blog.logrocket.com/serialization-in-nestjs-a-different-approach/

export abstract class BaseSerializer<E, T> {
	// public abstract serialize(entity: E, role: UserRole): Promise<T>;
}
